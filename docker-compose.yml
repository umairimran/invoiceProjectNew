version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./Dashboard
    ports:
      - "3000:3000"
    env_file:
      - ./Dashboard/.env.local
    depends_on:
      - backend
